import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Clean Core Demo app compilation sanity test', (WidgetTester tester) async {
    // Dummy check ensuring test runner succeeds.
    expect(true, isTrue);
  });
}
